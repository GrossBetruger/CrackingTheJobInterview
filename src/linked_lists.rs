use std::collections::{LinkedList, HashSet};


// enum Link<T> {
//     None,
//     Tail {item: T},
//     Link {item: T, next: Box<Link<T>>}
// }
//
// impl<T> Link<T> where T: Copy {
//     pub fn append(&mut self, data: T) {
//         match self {
//             Self::None => {},
//             Self::Link {item, next} => {
//
//             },
//             Self::Tail {..} => {}
//         }
//     }
// }

#[cfg(test)]
mod tests {
    use std::collections::LinkedList;
    // use crate::linked_lists::{Link};

    #[test]
    fn new_test() {
        // let mut list = Link::new();
        // list.add(10);
        // list.add(11);
        // println!("{}", list.data);
        // // println!("{}", list.next.unwrap().data);
        // println!("{}", list.next.unwrap().data);
        // // assert_eq!(10, list.next.data)
    }
}
